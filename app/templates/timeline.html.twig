{% extends 'base.html.twig' %}

{% block title %}Timeline - TweetToast{% endblock %}

{% block body %}
<h1>Timeline</h1>
<div class="tweets">
    {% for tweet in tweets %}
        <div class="tweet" id="tweet-{{ tweet.id }}">
            <p><strong>{{ tweet.author.pseudo }}</strong> - {{ tweet.date|date('Y-m-d H:i') }}</p>
            <p>{{ tweet.content }}</p>
            <p>
                <form method="POST" action="{{ path('like_tweet', {id: tweet.id}) }}" style="display:inline;">
                    <button type="submit" class="btn-icon" title="Liker ce tweet">
                        <i class="fa-regular fa-heart"></i>
                    </button>
                    {{ tweet.likes.count }}
                </form>
                &nbsp;&nbsp;
                <i class="fa-regular fa-comment"></i> {{ tweet.comments.count }}
            </p>
            <a href="{{ path('tweet_show', {'id': tweet.id}) }}" class="btn btn-primary">Voir le tweet</a>
        </div>

    {% endfor %}
</div>
{% endblock %}


{% block style %}
<style>
body {
    background-color: #f5f8fa;
    font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
    margin: 0;
    padding: 0;
}

h1 {
    text-align: center;
    color: #1da1f2;
    margin-top: 30px;
}

.tweets {
    max-width: 600px;
    margin: 20px auto;
    background-color: white;
    border-radius: 10px;
    box-shadow: 0 2px 6px rgba(0,0,0,0.1);
    overflow: hidden;
}

.tweet {
    padding: 20px;
    border-bottom: 1px solid #e6ecf0;
    transition: background-color 0.2s ease-in-out;
}

.tweet:hover {
    background-color: #f0f8ff;
}

.tweet p {
    margin: 5px 0;
}

.tweet p:first-child {
    font-size: 0.9rem;
    color: #657786;
}

.tweet strong {
    color: #14171a;
    font-weight: bold;
}

.tweet p:nth-child(2) {
    font-size: 1.1rem;
    color: #0f1419;
}

.tweet p:nth-child(3),
.tweet p:nth-child(4) {
    font-size: 0.85rem;
    color: #5b7083;
    display: inline-block;
    margin-right: 15px;
}

.tweet p:nth-child(3) i.fa-heart {
    vertical-align: middle;
    color: #e0245e; /* Couleur pour le like */
}

.tweet p:nth-child(3) {
    vertical-align: middle;
}

.tweet p:nth-child(3) i.fa-comment {
    color: #1da1f2; /* Couleur pour le commentaire */
    margin-left: 15px;
}

.tweet p:nth-child(3) {
    font-size: 0.9rem;
}

.btn.btn-primary {
    display: inline-block;
    margin-top: 10px;
    padding: 6px 12px;
    background-color: #1da1f2;
    color: white;
    border-radius: 20px;
    text-decoration: none;
    font-size: 0.9rem;
    transition: background-color 0.2s;
}

.btn.btn-primary:hover {
    background-color: #0d8ddb;
}

.btn-icon {
    background: none;
    border: none;
    cursor: pointer;
    color: #e0245e;
    font-size: 1rem;
    padding: 0;
}

.btn-icon:hover {
    color: #c2184b;
}

.btn-icon i {
    margin-right: 5px;
}
</style>
{% endblock %}
